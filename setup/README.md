# Setup

## How to use

Edit the TimeChecks.json file to include when the script should run.

Run the run.bat file to setup the windows scheduled tasks, to run the background service, at specified times.

## How it works

setup.ps1 uses the TimeChecks.json to create/modify the scheduled events, to run the background service at the specified times, within TimeChecks.json.
